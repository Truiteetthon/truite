
// Array of P --------------------------------------


const p1Images = [
'photo/P1/1.png',
'photo/P1/2.jpeg',
'photo/P1/3.png',
'photo/P1/4.png',
];

const P6Images = [
  'photo/P6/P6.jpg',
'photo/P6/P6-01.jpg',
'photo/P6/P6-02.jpg',
'photo/P6/P6-03.jpg',
'photo/P6/P6-04.jpg',
'photo/P6/P6-05.jpg',
'photo/P6/P6-06.jpg',
'photo/P6/P6-07.jpg',
];

const P7Images = [
  'photo/P7/P7-01.jpg',
  'photo/P7/P7-02.jpg',
  'photo/P7/P7-03.jpg',
  'photo/P7/P7-04.jpg',
  'photo/P7/P7-05.jpg',
  ];
  
  

const P9Images = [
  'photo/P9/P9.jpg',
'photo/P9/P9-01.jpg',
'photo/P9/P9-02.jpg',
'photo/P9/P9-03.jpg',
'photo/P9/P9-04.jpg',
'photo/P9/P9-05.jpg',
'photo/P9/P9-06.jpg',
];

const P10Images = [
  'photo/P10/P10.jpg',
'photo/P10/P10-01.jpg',
'photo/P10/P10-02.jpg',
];


const p11Images = [
  'photo/P11/P11.jpg',
'photo/P11/P11-0.jpg',
'photo/P11/P11-1.jpg',
'photo/P11/P11-2.jpg',
'photo/P11/P11-3.jpg',
'photo/P11/P11-4.jpg',
'photo/P11/P11-5.jpg',
];

const p12Images = [
  'photo/P12/P12.jpg',
'photo/P12/P12-1.jpg',
'photo/P12/P12-2.jpg',
'photo/P12/P12-3.jpg',
'photo/P12/P12-4.jpg',
'photo/P12/P12-5.jpg',
'photo/P12/P12-6.jpg',
'photo/P12/P12-7.jpg',
'photo/P12/P12-8.jpg',
'photo/P12/P12-9.jpg',
'photo/P12/P12-10.jpg',
];

const P13Images = [
  
  ];

const BertImages = [
'photo/Bert/Bert-01.jpg',
'photo/Bert/Bert-02.jpg',
'photo/Bert/Bert-03.jpg',
'photo/Bert/Bert-04.jpg',
'photo/Bert/Bert-05.jpg',
'photo/Bert/Bert-06.jpg',
'photo/Bert/Bert-07.jpg',
'photo/Bert/Bert-08.jpg',
'photo/Bert/Bert-09.jpg',
'photo/Bert/Bert-10.jpg',
'photo/Bert/Bert-11.jpg',
'photo/Bert/Bert-12.jpg',
];

const P14Images = [
  'photo/P14/P14-01.jpg',
  'photo/P14/P14-02.jpg',
  'photo/P14/P14-03.jpg',
  'photo/P14/P14-04.jpg',
  'photo/P14/P14-05.jpg',
  'photo/P14/P14-06.jpg',
  ];
  
  const P15Images = [
    'photo/P15/P15-1.jpg',
    'photo/P15/P15-2.jpg',
    'photo/P15/P15-3.jpg',
    'photo/P15/P15-4.jpg',
    'photo/P13/P13-1.jpg',
  'photo/P13/P13-2.jpg',
  'photo/P13/P13-3.jpg',
  'photo/P13/P13-4.jpg',
  'photo/P13/P13-5.jpg',
  'photo/P13/P13-6.jpg',
  'photo/P13/P13-7.jpg',

    ];
    
    const P16Images = [
      'photo/P16/P16-01.jpg',
      'photo/P16/P16-02.jpg',
      'photo/P16/P16-03.jpg',
      'photo/P16/P16-04.jpg',
      'photo/P16/P16-05.jpg',
      'photo/P16/P16-06.jpg',
      'photo/P16/P16-07.jpg',
      'photo/P16/P16-08.jpg',
      'photo/P16/P16-09.jpg',
      'photo/P16/P16-10.jpg',
      'photo/P16/P16-11.jpg',
      'photo/P16/P16-12.jpg',
      'photo/P16/P16-13.jpg',
      'photo/P16/P16-14.jpg',
      'photo/P16/P16-15.jpg',
      'photo/P16/P16-16.jpg',
      
      ];

      const P17Images = [
        'photo/P17/P17-01.jpg',
        'photo/P17/P17-02.jpg',
        'photo/P17/P17-03.jpg',
        'photo/P17/P17-04.jpg',
        'photo/P17/P17-05.jpg',
        'photo/P17/P17-06.jpg',
        'photo/P17/P17-07.jpg',
        'photo/P17/P17-08.jpg',
        'photo/P17/P17-09.jpg',
        
        
        ];

        
const P18Images = [
  'photo/P18/P18-01.jpg',
  'photo/P18/P18-02.jpg',
  'photo/P18/P18-03.jpg',  
  ];

        const P19Images = [
          'photo/P19/P19-01.jpg',
          'photo/P19/P19-02.jpg',
          'photo/P19/P19-03.jpg',
          'photo/P19/P19-04.jpg',
          'photo/P19/P19-05.jpg',
          'photo/P19/P19-06.jpg',
          'photo/P19/P19-07.jpg',
          'photo/P19/P19-08.jpg',
          'photo/P19/P19-09.jpg',
          'photo/P19/P19-10.jpg',
          'photo/P19/P19-11.jpg',
          'photo/P19/P19-12.jpg',
          'photo/P19/P19-13.jpg',
          'photo/P19/P19-14.jpg',
          'photo/P19/P19-15.jpg',
          'photo/P19/P19-16.jpg',
          'photo/P19/P19-17.jpg',
          'photo/P19/P19-18.jpg',
          'photo/P19/P19-19.jpg',
          'photo/P19/P19-20.jpg',
          'photo/P19/P19-21.jpg',
          'photo/P19/P19-22.jpg',
          'photo/P19/P19-23.jpg',
          'photo/P19/P19-24.jpg',
          'photo/P19/P19-25.jpg',
          'photo/P19/P19-26.jpg',
          'photo/P19/P19-27.jpg',
          'photo/P19/P19-28.jpg',
          'photo/P19/P19-29.jpg',
          'photo/P19/P19-30.jpg',
          'photo/P19/P19-31.jpg',
          'photo/P19/P19-32.jpg',
          'photo/P19/P19-33.jpg',
          'photo/P19/P19-34.jpg',
          'photo/P19/P19-35.jpg',
          'photo/P19/P19-36.jpg',
          'photo/P19/P19-37.jpg',
          'photo/P19/P19-38.jpg',
          'photo/P19/P19-39.jpg',
          'photo/P19/P19-40.jpg',
          'photo/P19/P19-41.jpg',
          
          ];

// Code of P --------------------------------------
  
// P1 -----------------

let p1Index = 0;

// Get the image element
const p1 = document.getElementById('p1');

// Function to change the image
const p1changeImage = () => {
  // Increment the index to point to the next image
  p1Index++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (p1Index >= p1Images.length) {
      p1Index = 0;
  }

  // Update the image source to the new image
  p1.src = p1Images[p1Index];
};

// Add event listeners for both click and touchstart
p1.addEventListener('click', p1changeImage);




// P6 ---------------------------
let P6currentIndex = 0;

// Get the image element
const P6 = document.getElementById('P6');

// Function to change the image
const P6changeImage = () => {
  // Increment the index to point to the next image
  P6currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P6currentIndex >= P6Images.length) {
      P6currentIndex = 0;
  }

  // Update the image source to the new image
  P6.src = P6Images[P6currentIndex];
};

// Add event listeners for both click and touchstart
P6.addEventListener('click', P6changeImage);



// P7 ---------------------------
let P7currentIndex = 0;

// Get the image element
const P7 = document.getElementById('P7');

// Function to change the image
const P7changeImage = () => {
  // Increment the index to point to the next image
  P7currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P7currentIndex >= P7Images.length) {
      P7currentIndex = 0;
  }

  // Update the image source to the new image
  P7.src = P7Images[P7currentIndex];
};

// Add event listeners for both click and touchstart
P7.addEventListener('click', P7changeImage);

// P9 ---------------------------
let P9currentIndex = 0;

// Get the image element
const P9 = document.getElementById('P9');

// Function to change the image
const P9changeImage = () => {
  // Increment the index to point to the next image
  P9currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P9currentIndex >= P9Images.length) {
      P9currentIndex = 0;
  }

  // Update the image source to the new image
  P9.src = P9Images[P9currentIndex];
};

// Add event listeners for both click and touchstart
P9.addEventListener('click', P9changeImage);




// P10 ---------------------------
let P10currentIndex = 0;

// Get the image element
const P10 = document.getElementById('P10');

// Function to change the image
const P10changeImage = () => {
  // Increment the index to point to the next image
  P10currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P10currentIndex >= P10Images.length) {
      P10currentIndex = 0;
  }

  // Update the image source to the new image
  P10.src = P10Images[P10currentIndex];
};

// Add event listeners for both click and touchstart
P10.addEventListener('click', P10changeImage);




// P11 ---------------------------
let p11currentIndex = 0;

// Get the image element
const p11 = document.getElementById('p11');

// Function to change the image
const p11changeImage = () => {
  // Increment the index to point to the next image
  p11currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (p11currentIndex >= p11Images.length) {
      p11currentIndex = 0;
  }

  // Update the image source to the new image
  p11.src = p11Images[p11currentIndex];
};

// Add event listeners for both click and touchstart
p11.addEventListener('click', p11changeImage);








// P12 ---------------------------
let p12currentIndex = 0;

// Get the image element
const p12 = document.getElementById('p12');

// Function to change the image
const p12changeImage = () => {
  // Increment the index to point to the next image
  p12currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (p12currentIndex >= p12Images.length) {
      p12currentIndex = 0;
  }

  // Update the image source to the new image
  p12.src = p12Images[p12currentIndex];
};

// Add event listeners for both click and touchstart
p12.addEventListener('click', p12changeImage);




// P14 ---------------------------
let P14currentIndex = 0;

// Get the image element
const P14 = document.getElementById('P14');

// Function to change the image
const P14changeImage = () => {
  // Increment the index to point to the next image
  P14currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P14currentIndex >= P14Images.length) {
      P14currentIndex = 0;
  }

  // Update the image source to the new image
  P14.src = P14Images[P14currentIndex];
};

// Add event listeners for both click and touchstart
P14.addEventListener('click', P14changeImage);



// P15 ---------------------------
let P15currentIndex = 0;

// Get the image element
const P15 = document.getElementById('P15');

// Function to change the image
const P15changeImage = () => {
  // Increment the index to point to the next image
  P15currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P15currentIndex >= P15Images.length) {
      P15currentIndex = 0;
  }

  // Update the image source to the new image
  P15.src = P15Images[P15currentIndex];
};

// Add event listeners for both click and touchstart
P15.addEventListener('click', P15changeImage);

// P16 ---------------------------
let P16currentIndex = 0;

// Get the image element
const P16 = document.getElementById('P16');

// Function to change the image
const P16changeImage = () => {
  // Increment the index to point to the next image
  P16currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P16currentIndex >= P16Images.length) {
      P16currentIndex = 0;
  }

  // Update the image source to the new image
  P16.src = P16Images[P16currentIndex];
};

// Add event listeners for both click and touchstart
P16.addEventListener('click', P16changeImage);





// P17 ---------------------------
let P17currentIndex = 0;

// Get the image element
const P17 = document.getElementById('P17');

// Function to change the image
const P17changeImage = () => {
  // Increment the index to point to the next image
  P17currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P17currentIndex >= P17Images.length) {
      P17currentIndex = 0;
  }

  // Update the image source to the new image
  P17.src = P17Images[P17currentIndex];
};

// Add event listeners for both click and touchstart
P17.addEventListener('click', P17changeImage);


// P18 ---------------------------
let P18currentIndex = 0;

// Get the image element
const P18 = document.getElementById('P18');

// Function to change the image
const P18changeImage = () => {
  // Increment the index to point to the next image
  P18currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P18currentIndex >= P18Images.length) {
      P18currentIndex = 0;
  }

  // Update the image source to the new image
  P18.src = P18Images[P18currentIndex];
};

// Add event listeners for both click and touchstart
P18.addEventListener('click', P18changeImage);


// P19 ---------------------------
let P19currentIndex = 0;

// Get the image element
const P19 = document.getElementById('P19');

// Function to change the image
const P19changeImage = () => {
  // Increment the index to point to the next image
  P19currentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (P19currentIndex >= P19Images.length) {
      P19currentIndex = 0;
  }

  // Update the image source to the new image
  P19.src = P19Images[P19currentIndex];
};

// Add event listeners for both click and touchstart
P19.addEventListener('click', P19changeImage);


// Bert ---------------------------
let BertcurrentIndex = 0;

// Get the image element
const Bert = document.getElementById('Bert');

// Function to change the image
const BertchangeImage = () => {
  // Increment the index to point to the next image
  BertcurrentIndex++;

  // If the index goes beyond the array length, reset it to 0 (loop back to the first image)
  if (BertcurrentIndex >= BertImages.length) {
      BertcurrentIndex = 0;
  }

  // Update the image source to the new image
  Bert.src = BertImages[BertcurrentIndex];
};

// Add event listeners for both click and touchstart
Bert.addEventListener('click', BertchangeImage);





